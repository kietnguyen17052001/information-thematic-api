package com.example.informationthematicbackend.common.constaint;

public enum ErrorCode {
    ALREADY_EXIST,
    MISSING_VALUE,
    NOT_FOUND,
    INVALID_VALUE,
    DUPLICATE_LESSON,
    DUPLICATE_TIME,
    DUPLICATE_VALUE;
}
